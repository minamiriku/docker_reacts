{
  "scripts": {
    "start": "cd react-app && PORT=5555 yarn start",
    "build": "cd react-app && yarn build",
    "test": "cd react-app && yarn test",
    "eject": "cd react-app && yarn eject"
  }
}
